# ML-014 Agent Status

- Task ID: ML-014
- Active role: **Tester**
- Status: **CANDIDATE — tester PASS; reviewer pending**
- Branch/worktree: `main` @ `b5f1857`
- Latest commit: `f653334` (ML-014 implementation)
- Blocking issue: none for tester gate

## Gate status

| Gate | Result |
|------|--------|
| Implementer | done (`f653334`) |
| Reviewer | pending |
| Tester | **PASS** |

## Tester pass (2026-08-21)

Commands:

```text
python -m pytest tests/unit/evaluation/ -q          → 60 passed
python -m pytest tests/leakage/ -q                 → 42 passed
python -m pytest -q                                → 947 passed, 6 xfailed
python -m compileall -q scripts/evaluate_locked_calibration.py
```

Adversarial coverage verified by existing tests:

- chronological inner-training calibrator partition
- evaluation-label isolation from calibrator fit
- explicit 2026 exclusion in harness
- probability bounds and serialization round-trip
- degenerate calibration partition rejected

Not run here: live `evaluate_locked_calibration.py` against `data/mlb.duckdb`
(gitignored on this checkout). Offline unit + leakage coverage is sufficient for
tester APPROVE.

## Recommendation (unchanged)

**KEEP RAW** — no ADR-006 production promotion authorized.

## Next step

Dispatch Reviewer against `f653334` on `main`. After reviewer APPROVE, mark
ML-014 done in `tasks/index.md` and `state/CURRENT.md` (still research-only).
