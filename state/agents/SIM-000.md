# SIM-000 agent status

| Field | Value |
|---|---|
| Task | SIM-000 |
| Role | implementer |
| Status | CANDIDATE |
| Branch | `agent/SIM-000-game-level-simulation` |
| Worktree | `../predictions-1-wt-SIM-000` |
| Started | 2026-08-14 |
| Latest commit | `9e0fd55` |
| Tests | `pytest tests/unit/simulation/ -q` → **11 passed** |
| Blocker | none |

## Activity

Implemented game-level team score Monte Carlo:

- `src/simulation/score_model.py` — Poisson run-rate models on documented pregame
  feature subsets; separate fit path from inference.
- `src/simulation/game_level.py` — `simulate_game` / `simulate_games` with
  `SimulationConfig` (10k trials default, deterministic `random_state`), tie
  handling (0.5 home win), optional `store_trials` for SIM-002.
- `tests/unit/simulation/test_game_level.py` — deterministic seed, means,
  tie rule, missing-feature errors, post-game column isolation.

## Known limitations

- Independent Poisson home/away draws (no joint correlation / NegBin overdispersion).
- Small fixed 4-feature subset per side; full 240-column Gold matrix unused.
- `score_model` is a required kwarg on simulate APIs (fitted via `fit_score_model`
  on a chronological training partition by callers; not wired to daily operator).
- No walk-forward validation harness yet (SIM-001 scope).

## Next gates

- Reviewer after CANDIDATE
- Tester after CANDIDATE

## Downstream

- **SIM-001** can proceed once this branch merges (stable `simulate_game` contract).
- **SIM-002** can proceed; `store_trials=True` exposes trial vectors for totals math.
