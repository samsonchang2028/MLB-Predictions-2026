# APP-002 Agent Status

- Task ID: APP-002
- Active role: implementer
- Status: DONE - performance dashboard updated for final 2026 holdout evidence
- Worktree: none (completed directly on `main` at operator request)

## Summary

APP-002 now exposes historical prediction quality and final holdout evidence in
Streamlit without recomputing model, market, journal, or ROI metrics.

Implemented/updated:

- `src/app/performance.py`
  - keeps development model/window ranking loaders for the repaired 2021-2025
    experiment report;
  - keeps calibration/reliability comparison loaders for the repaired
    development report;
  - adds `load_holdout_summary(report)` for ML-010 final holdout metrics;
  - adds `load_holdout_predictions(report)` for game_pk-keyed 2026 holdout
    predictions;
  - labels development evidence separately from final holdout evidence;
  - continues omitting market-relative edge/EV and simulated ROI because those
    values are not persisted in the experiment reports or OBS-001 journal.
- `src/app/performance_page.py`
  - displays final 2026 holdout metrics first when
    `reports/experiments/v1-holdout-2026.json` exists;
  - displays game-level 2026 holdout predictions in an expander;
  - displays development model/window and calibration tables separately;
  - keeps OBS-001 prediction history display;
  - supports `HOLDOUT_REPORT_PATH`, `PERFORMANCE_REPORT_PATH`, and
    `PREDICTION_JOURNAL_PATH` overrides.
- `tests/unit/app/test_performance.py`
  - adds final holdout summary/prediction loader tests;
  - keeps guards proving development loaders do not read the old repaired-report
    `holdout_2026` placeholder;
  - verifies all displayed values are passed through verbatim and labeled.

## Commands / tests run

- `python -m pytest tests/unit/app -q` -> **20 passed, 1 xfailed**.
- `python -m py_compile src/app/performance.py src/app/performance_page.py` -> **PASS**.

## Known limitations

- Market-relative metrics and simulated ROI are shown as unavailable rather than
  computed in the dashboard. This is intentional: APP-002 forbids recomputing
  model/market metrics inside Streamlit, and no current committed report stores
  final market-relative values.
- Prediction history depends on an OBS-001 journal file existing at
  `state/predictions/journal.jsonl` or `PREDICTION_JOURNAL_PATH`.

## ADR / project-state

No ADR change required. APP-002 is complete; next work can be packaging/running
Streamlit locally or adding a future persisted market-evaluation artifact if the
project wants market-relative dashboard sections.
