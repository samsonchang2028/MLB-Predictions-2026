# SIM-003 agent status

| Field | Value |
|---|---|
| Task | SIM-003 |
| Role | implementer |
| Status | CANDIDATE |
| Branch | `agent/SIM-003-full-gold-score-model` |
| Worktree | `../predictions-1-wt-SIM-003` |
| Blocker | none |

## Summary

Upgraded `fit_score_model` to fit home/away Poisson regressors on the full sorted
Gold feature column union (via `evaluation.runner._feature_columns` / `_as_float`),
matching the XGBoost `vectorize_matrix` contract. Both models share one
`feature_names` list; optional `feature_columns=` override for PIPE-007 alignment.
`simulate_game` API unchanged.

## Tests

```text
python -m pytest tests/unit/simulation/ -q
14 passed in 4.08s
```

## Feature count / lambda example

Synthetic 14-column Gold fixture (8 run-rate + 6 starter/bullpen): changing
`diff_starter_era` from 0.0 to 1.5 shifts `lambda_home` from ~4.86 to ~5.15
(home model uses full feature vector).

## Training row shape (PIPE-007)

```python
{
    "features": {<sorted Gold feature keys, no score columns>},
    "home_runs": int,  # label, top-level
    "away_runs": int,  # label, top-level
}
```

Production Gold rows carry ~240 feature keys; pass the matrix `feature_columns`
list from a certified Gold build when fitting for schema stability.

## Files changed

- `src/simulation/score_model.py`
- `tests/unit/simulation/test_game_level.py`
- `tests/unit/simulation/test_score_model_full_gold.py` (new)

## Follow-ups

- Reviewer + Tester gates per task file.
