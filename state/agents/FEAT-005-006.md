# FEAT-005 / FEAT-006 Agent Status

- Task IDs: FEAT-005 then FEAT-006 (sequential: same file surface)
- Active role: Reviewer + Tester (dispatched against the implementer candidate)
- Status: REVIEWING / TESTING (FEAT-005 CANDIDATE; FEAT-006 CANDIDATE; gate in progress)
- Branch: `agent/FEAT-005-006-gold-completeness`
- Worktree: `C:\Users\sfkim\OneDrive\Desktop\sideproj\predictions-1-wt-FEAT-005-006`
- FEAT-005: candidate commit `ee50ec0` encodes explicit, returned exclusions and
  preserves one-sided-component failures. Classification was re-audited against
  the read-only 2021-2025 historical database at `data/mlb.duckdb`:
  - `632457`: legitimate exclusion; Silver is `Final/Cancelled` (`CI`), scores
    and winners are NULL, and a Bronze detail payload was fetched.
  - `746577`: legitimate exclusion; Silver is `Final/Cancelled` (`CR`), scores
    and winners are NULL, and a Bronze detail payload was fetched.
  - `746596`: ingestion defect owned by DATA-016; Silver is a completed 4-9
    `Final`, but the detail attempt failed with DNS `ConnectionError` and no
    Bronze detail payload exists.
  - `746597`: ingestion defect owned by DATA-016; Silver is a completed 3-5
    `Final`, but the detail attempt failed with DNS `ConnectionError` and no
    Bronze detail payload exists.
  No 2026 rows or performance were inspected.
- FEAT-006: Gold pre-model completeness gate with team/starter/bullpen/rest family
  reports; absent required family BLOCKS ML experiments; no auto-dropping.
  Implemented a strict historical default plus explicit inference WARN mode for
  PIPE-001, per-column coverage/distribution diagnostics, explicit V1 required
  columns, and evaluator/calibration enforcement of historical PASS.
- Historical diagnostic (read-only, 2021-2025 only): rebuilt the existing hollow
  feature artifact in memory and the new gate returned FAIL over 12,146 rows.
  Team PASS; starter FAIL (38 all-null required columns); bullpen FAIL (12
  all-null); rest/schedule FAIL (8 all-null). Missing required differentials and
  constant-zero pitching/workload columns were also detected. This confirms the
  former 58 dead pitching columns cannot silently reach ML again.
- Tests:
  - focused feature/pipeline/evaluation/calibration/leakage: 159 passed
  - full repository: 468 passed
  - interpreter: main worktree `.venv` (the system Python lacks XGBoost)
- Known limitation / downstream gate: DATA-016 must repair/re-ingest and DATA-017
  must recertify before a rebuilt historical Gold matrix can demonstrate PASS.
  No Gold artifact was persisted and no ML experiment was run by this task.
- Reviewer/tester follow-up: required by both task files; no open finding is known
  at implementer handoff.
- ADR/state: ADR-005 already governs the change. `state/CURRENT.md` should be
  updated by the Orchestrator after merge and after repaired Gold PASS exists.
- Blocking issue: None in implementation. Downstream model work remains blocked
  on repaired data + Silver certification + Gold historical completeness PASS.

## Reviewer + Tester gate — both PASS

**Reviewer verdict: APPROVE.** No P0/P1. Independently queried `data/mlb.duckdb`
(not the implementer's notes) and confirmed `silver.pitcher_appearances` was
still 100% NULL on stat columns at review time (pre-DATA-016-merge state) and
that all 4 game_pks classify as claimed (632457/746577 Cancelled with fetched-
but-empty payloads; 746596/746597 completed Final with a DNS ConnectionError
and no Bronze payload). Cross-checked `REQUIRED_FAMILY_COLUMNS` in
`src/features/completeness.py` against the actual column names emitted by
`team.py`/`starter.py`/`bullpen.py` — all match, a real allowlist not a
speculative one. Confirmed via `grep` that the 4 known game_pks appear only in
docstrings, never in executable logic (`_game_component_values` keys purely on
observed component presence). Confirmed the gate genuinely blocks
(`FeatureCompletenessError` raised in historical mode; evaluator + calibration
call `require_historical_feature_completeness` as defense-in-depth) and that
nothing is silently dropped (every `continue` pairs with an explicit
`excluded`/`missing_components` report). Ran the full suite: 473 passed. One
non-blocking P3: commit sequencing between `ee50ec0`/`8348dd5` is slightly
confusing (the audit narrative landed in the first commit; the second only
touches the status file) — cosmetic.

**Tester verdict: LOOKS_SAFE_TO_MERGE.** Re-ran suites: 151/434 after
additions. Probed: 99%-null family (correctly classified
`implausibly_low_population`, hard-fails as designed per the documented 50%
floor, distinct from `entirely_null`); single non-null row among thousands
(same, not conflated); all-healthy-except-one-empty-family end-to-end through
`build_feature_matrix` (FAILs naming the specific family, others stay PASS);
and the key regression risk named in the task brief — an unseen 5th broken
game (synthetic `game_pk=919191`, zero starter+bullpen coverage) is excluded
via the same generic observable-coverage rule as the 4 known games, visibly
reported, not silently dropped or crashed. Added 5 regression tests across
`test_completeness.py`/`test_build.py` (committed `dede6f0`, test-only), all
pass. The real-2021-2025-build FAIL claim (team PASS; starter/bullpen/rest
FAIL) was not independently re-run by the tester (DB absent from that
worktree) but is reasoned-consistent with the code and was independently
confirmed by the reviewer via direct DB query.

**Orchestrator decision: MERGE.** No P0/P1 from either gate; the one P3 is
non-blocking and recorded here. Merged to `main`.
