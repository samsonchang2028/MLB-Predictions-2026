# APP-016 Context Pack — CLV Monitor Page

## Mission

Streamlit page for **live CLV observability** — reads homelab JSONL artifacts,
shows strict-close metrics without running CLI manually. Display only.

## Partial work (orchestrator started — implementer must review/finish)

These files may exist on disk from an aborted direct implementation:

- `src/app/clv_monitor.py` — `build_clv_dashboard`, `summarize_odds_closes`
- `src/app/clv_monitor_page.py` — Streamlit UI
- `pages/9_CLV_Monitor.py` — wrapper

**Implementer:** verify correctness, add tests, task doc alignment, README row,
fix any gaps. Do not assume complete.

## Host facts (2026-09-14)

- Closing-odds timer **active**
- `odds_closes.jsonl`: ~161 rows (52 live, 109 backfill)
- Latest prospective CLV (latest anchor): strict N=43, Case 2
- No CLV in Streamlit today — CLI only (`scripts/market_clv_study.py`)

## Reuse

```python
from market.clv_evaluation import build_clv_report, load_clv_population
from market.odds_closes import prediction_anchor_label
```

Default paths (match CLI):

- `state/predictions/daily.jsonl`
- `state/predictions/journal.jsonl`
- `state/predictions/odds_books.jsonl`
- `state/predictions/odds_closes.jsonl`

Env overrides: `PREDICTIONS_STORE_PATH`, `PREDICTION_JOURNAL_PATH` (existing);
add `ODDS_CLOSES_PATH` if useful (page already uses this name).

## UI spec

| Section | Content |
|---------|---------|
| Disclaimer | CLV observational; ADR-007 blocked |
| How-to expander | CLV formula, strict window, anchor meanings, 5-min cache |
| Capture health | total / live / backfill / latest snapshot |
| Strict CLV metrics | N, mean CLV (pp), +CLV rate, strict ROI, verdict |
| Secondary | pregame-latest N/mean, full-pop ROI |
| Rejections table | from `rejection_summary` |
| Recent strict games | table, limit ~25 |

Sidebar: anchor radio, Refresh now (clears cache).

## Tests (minimum)

- `summarize_odds_closes` live/backfill counts
- `build_clv_dashboard` with fixture from `tests/unit/market/test_clv.py` helpers
- `format_strict_clv_rows` display shaping
- empty/missing odds_closes graceful

## Out of scope

- Scheduled report writes to `reports/market/`
- Changing CLV methodology
- Auto systemd / operator changes

## Gates

Implementer → Reviewer → Tester → merge (orchestrator owns merge)
